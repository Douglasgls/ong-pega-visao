---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`w-full py-8 flex items-center justify-center ${className}`}>
  <div class="w-full h-px bg-gradient-to-r from-transparent via-zinc-800 to-transparent"></div>
</div>
